import Link from "next/link";
import { requireAdmin } from "@/lib/auth";
import { adminLogout } from "@/app/admin/actions";
export default async function AdminLayout({ children }: { children: React.ReactNode }) { const admin = await requireAdmin(); return <main className="mx-auto min-h-screen max-w-6xl px-6 py-8"><header className="flex flex-wrap items-center justify-between gap-4 border-b border-zinc-800 pb-5"><div><p className="font-semibold tracking-wide">VOLITEX AI <span className="text-zinc-500">/ OPERATIONS</span></p><p className="mt-1 text-xs text-zinc-500">{admin.email}</p></div><nav className="flex items-center gap-4 text-sm text-zinc-300"><Link href="/admin/clients">Clients</Link><Link href="/admin/activation">Activation queue</Link><form action={adminLogout}><button className="text-zinc-400">Sign out</button></form></nav></header>{children}</main>; }
