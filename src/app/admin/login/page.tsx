import { adminLogin } from "@/app/admin/actions";
export default async function AdminLogin({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const { error } = await searchParams; return <main className="shell"><form action={adminLogin} className="panel narrow space-y-5"><p className="eyebrow">VOLITEX AI / OPERATIONS</p><h1>Operator sign in</h1>{error && <p role="alert" className="text-amber-200">{error}</p>}<label className="block text-sm">Email<input required name="email" type="email" className="mt-2 w-full rounded-md border border-zinc-700 bg-zinc-900 p-3" /></label><label className="block text-sm">Password<input required name="password" type="password" className="mt-2 w-full rounded-md border border-zinc-700 bg-zinc-900 p-3" /></label><button className="rounded-md bg-zinc-100 px-4 py-2.5 font-medium text-zinc-950">Sign in</button></form></main>; }
