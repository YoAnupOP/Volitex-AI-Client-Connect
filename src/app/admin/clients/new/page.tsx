import { createClient } from "@/app/admin/actions";
import { requireAdmin } from "@/lib/auth";
export default async function NewClient({ searchParams }: { searchParams: Promise<{ error?: string }> }) { await requireAdmin(); const { error } = await searchParams; return <section className="py-10"><p className="eyebrow">NEW CLIENT</p><h1 className="mt-2 text-3xl font-semibold">Set up a connection</h1><p className="mt-2 text-zinc-400">Create the client record first, then share one secure link.</p>{error && <p role="alert" className="mt-5 text-amber-200">{error}</p>}<form action={createClient} className="mt-8 grid gap-6 lg:grid-cols-[1fr_300px]"><section className="space-y-5 rounded-xl border border-zinc-800 bg-zinc-950 p-6"><h2 className="font-semibold">1. Client profile</h2><div className="grid gap-4 sm:grid-cols-2"><Field label="Business/client name" name="clientName" required /><Field label="Contact name" name="contactName" /><Field label="Contact email" name="email" type="email" required /><label className="text-sm">Client type<select name="clientType" className="mt-2 w-full rounded-md border border-zinc-700 bg-zinc-900 p-3"><option value="real_estate">Real estate</option><option value="creator">Creator/influencer</option><option value="other">Other</option></select></label><Field label="Market/country" name="market" placeholder="IN" /><Field label="IANA time zone" name="timezone" placeholder="Asia/Kolkata" /><Field label="Preferred language" name="locale" placeholder="Optional" /><Field label="Sales owner" name="owner" placeholder="Optional" /></div><label className="block text-sm">Internal note<textarea name="note" className="mt-2 min-h-20 w-full rounded-md border border-zinc-700 bg-zinc-900 p-3" /></label><h2 className="pt-3 font-semibold">2. Service purchased</h2><label className="block text-sm">Connection scope<select name="scope" defaultValue="whatsapp" className="mt-2 w-full rounded-md border border-zinc-700 bg-zinc-900 p-3"><option value="whatsapp">WhatsApp automation</option><option value="instagram">Instagram automation</option><option value="both">Both channels</option></select></label><h2 className="pt-3 font-semibold">3. Access</h2><p className="text-sm leading-6 text-zinc-400">Generating the client creates a secure, single-use link. You’ll be able to copy it once.</p><button className="rounded-md bg-zinc-100 px-4 py-2.5 text-sm font-semibold text-zinc-950">Generate access link</button></section><aside className="h-fit rounded-xl border border-zinc-800 p-6"><p className="text-sm font-semibold">Client preview</p><p className="mt-3 text-sm leading-6 text-zinc-400">Your client will see only the Meta service they bought, a short explanation before Meta opens, and a calm status while Volitex completes the technical setup.</p></aside></form></section>; }
function Field({ label, name, type = "text", required = false, placeholder }: { label: string; name: string; type?: string; required?: boolean; placeholder?: string }) { return <label className="block text-sm">{label}<input required={required} name={name} type={type} placeholder={placeholder} className="mt-2 w-full rounded-md border border-zinc-700 bg-zinc-900 p-3" /></label>; }
