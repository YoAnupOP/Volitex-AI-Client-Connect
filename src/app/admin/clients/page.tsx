import Link from "next/link";
import { requireAdmin } from "@/lib/auth";
import { database } from "@/lib/supabase";
export default async function ClientsPage({ searchParams }: { searchParams: Promise<{ q?: string }> }) { await requireAdmin(); const { q = "" } = await searchParams; let query = database().from("tenants").select("id, client_name, primary_contact_name, primary_contact_email, market_code, timezone, portal_access_paused, created_at, tenant_services(provider,status), client_access_tokens(created_at,used_at,expires_at,revoked_at)").order("created_at", { ascending: false }); if (q) query = query.ilike("client_name", `%${q}%`); const { data: tenants } = await query; return <section className="py-10"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">CLIENTS</p><h1 className="mt-2 text-3xl font-semibold">Connection clients</h1><p className="mt-2 text-zinc-400">A focused list of people waiting to connect or activate.</p></div><Link className="rounded-md bg-zinc-100 px-4 py-2.5 text-sm font-semibold text-zinc-950" href="/admin/clients/new">New client</Link></div><form className="mt-7"><label className="sr-only" htmlFor="q">Search clients</label><input id="q" name="q" defaultValue={q} placeholder="Search business name" className="w-full max-w-sm rounded-md border border-zinc-700 bg-zinc-900 px-3 py-2.5" /></form><div className="mt-6 overflow-x-auto rounded-xl border border-zinc-800"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-zinc-900 text-zinc-400"><tr><th className="p-4">Client</th><th>Market</th><th>Services</th><th>Access</th><th>Lifecycle</th><th>Last activity</th></tr></thead><tbody>{(tenants ?? []).map((tenant) => { const links = tenant.client_access_tokens ?? []; const latest = links.sort((a, b) => b.created_at.localeCompare(a.created_at))[0]; const access = tenant.portal_access_paused ? "Paused" : !latest ? "Not sent" : latest.used_at ? "Used" : latest.revoked_at || new Date(latest.expires_at) < new Date() ? "Expired" : "Sent"; const services = tenant.tenant_services ?? []; return <tr key={tenant.id} className="border-t border-zinc-800 hover:bg-zinc-900/50"><td className="p-4"><Link className="font-medium hover:underline" href={`/admin/clients/${tenant.id}`}>{tenant.client_name}</Link><p className="mt-1 text-zinc-500">{tenant.primary_contact_name || tenant.primary_contact_email || "—"}</p></td><td>{[tenant.market_code, tenant.timezone].filter(Boolean).join(" · ") || "—"}</td><td>{services.map((s) => s.provider === "whatsapp" ? "WhatsApp" : "Instagram").join(", ") || "—"}</td><td>{access}</td><td>{services.map((s) => s.status.replaceAll("_", " ")).join(", ") || "—"}</td><td>{latest ? new Intl.DateTimeFormat("en", { dateStyle: "medium" }).format(new Date(latest.created_at)) : "—"}</td></tr>; })}</tbody></table></div></section>; }
